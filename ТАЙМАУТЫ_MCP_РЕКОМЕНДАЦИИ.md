# ⏱️ РЕКОМЕНДУЕМЫЕ ТАЙМАУТЫ ДЛЯ MCP BROWSER

## 🎯 ОПТИМАЛЬНЫЕ ЗНАЧЕНИЯ

### BetBoom:
```
mcp_browsermcp_browser_navigate(url="https://betboom.ru/...")
mcp_browsermcp_browser_wait(time=3)
```
**3 секунды** - список загружается быстро

---

### Scores24 - Список матчей:
```
mcp_browsermcp_browser_navigate(url="https://scores24.live/ru/soccer?matchesFilter=live")
mcp_browsermcp_browser_wait(time=5)
```
**5 секунд** - список с фильтрами

---

### Scores24 - Страница матча:
```
mcp_browsermcp_browser_navigate(url="https://scores24.live/ru/soccer/m-...")
mcp_browsermcp_browser_wait(time=8)
```
**8-10 секунд** - много данных: статистика, H2H, форма, графики

---

## 📊 ПОЧЕМУ ИМЕННО ЭТИ ЗНАЧЕНИЯ

### BetBoom - 3 сек:
- ✅ Простой список матчей
- ✅ Минимум JS
- ✅ Быстро грузится

### Scores24 список - 5 сек:
- ✅ Фильтры live
- ✅ Много матчей
- ✅ Средняя сложность

### Scores24 страница матча - 8-10 сек:
- ⚠️ Много данных:
  - Статистика матча (xG, владение, удары)
  - История встреч (H2H)
  - Последние матчи обеих команд
  - Графики
  - Коэффициенты букмекеров
  - Чат
- ⚠️ Динамическая загрузка JS
- ⚠️ API запросы в фоне

**8 секунд** - минимум для полной загрузки  
**10 секунд** - с запасом для ночного времени

---

## 🕐 КОГДА ИСПОЛЬЗОВАТЬ РАЗНЫЕ ТАЙМАУТЫ

### Дневное время (12:00-20:00):
```
Список: 5 сек
Матч: 8 сек
```
✅ Серверы быстрые, мало задержек

### Вечернее время (20:00-00:00):
```
Список: 5 сек
Матч: 8-10 сек
```
⚠️ Больше нагрузка, возможны задержки

### Ночное время (00:00-06:00):
```
Список: 5-7 сек
Матч: 10-12 сек
```
⚠️⚠️ Медленные серверы, возможны таймауты

---

## 💡 УМНАЯ СТРАТЕГИЯ

### С retry (повторной попыткой):

```python
# Попытка 1
mcp_browsermcp_browser_navigate(url=match_url)
mcp_browsermcp_browser_wait(time=8)

snapshot = mcp_browsermcp_browser_snapshot()

# Если snapshot пустой или ошибка
if not snapshot or error:
    print("⚠️ Первая попытка не удалась, повторяю...")
    mcp_browsermcp_browser_wait(time=5)  # Еще подождать
    snapshot = mcp_browsermcp_browser_snapshot()
    
    if not snapshot:
        print("❌ Матч недоступен, пропускаем")
        return None
```

---

## 📋 ИТОГОВЫЕ РЕКОМЕНДАЦИИ

### Основные таймауты (в инструкциях):

**Для AI в AutoHotkey запросе:**
```
B) Найди нужный матч в списке и КЛИКНИ на него

C) Дождись полной загрузки страницы матча (таймаут 8-10 сек)
   ⚡ Для ускорения можешь отключить:
      • Анимации
      • Тяжелые изображения
      • Видео и рекламу

D) Получить snapshot
```

**В коде:**
```python
# После открытия списка
mcp_browsermcp_browser_wait(time=5)

# После открытия страницы матча
mcp_browsermcp_browser_wait(time=10)  # С запасом!
```

---

## 🎯 ПОЧЕМУ 8-10 СЕКУНД ОПТИМАЛЬНО

### ❌ Слишком мало (3-5 сек):
- Страница не успевает загрузиться
- Нет статистики в snapshot
- Таймауты (как в 00:57)

### ✅ Оптимально (8-10 сек):
- Страница полностью загружается
- Вся статистика доступна
- Редкие таймауты
- Не слишком медленно

### ⚠️ Слишком много (15-20 сек):
- Излишне долго
- 4 матча × 20 сек = 80 сек только ожидание
- Нет смысла, 10 секунд достаточно

---

## 🔧 ГДЕ ПРИМЕНЯЕТСЯ

### В AutoHotkey запросе (обновлено):
```
C) Дождись полной загрузки страницы матча (таймаут 8-10 сек)
```

### В вашем коде (когда используете):
```python
from hybrid_mcp_scores24_collector import MCPScores24Parser

# Открыть страницу матча
mcp_browsermcp_browser_navigate(url=match_url)

# ПОДОЖДАТЬ 10 сек (с запасом)
mcp_browsermcp_browser_wait(time=10)

# Получить snapshot
snapshot = mcp_browsermcp_browser_snapshot()

# Парсить
parser = MCPScores24Parser()
stats = parser.parse_match_snapshot(snapshot)
```

---

## ✅ ОБНОВЛЕНО В AUTOHOTKEY

**Было:**
```
C) Дождись полной загрузки страницы матча (таймаут 3-5 сек)
4) ПОДОЖДАТЬ 5 сек (полная загрузка)
```

**Стало:**
```
C) Дождись полной загрузки страницы матча (таймаут 8-10 сек)
4) ПОДОЖДАТЬ 8-10 сек (полная загрузка страницы матча)
```

---

## 🎊 ИТОГО

**Scores24 работает отлично!**

- ✅ MCP Browser открывает без проблем
- ✅ У вас в браузере тоже работает
- ⚠️ Таймаут в 00:57 = разовый сбой в ночное время
- ✅ **8-10 секунд** - оптимальный таймаут

**С новым таймаутом будет еще надежнее!** 💪

**Файл уже обновлен!** ✅
