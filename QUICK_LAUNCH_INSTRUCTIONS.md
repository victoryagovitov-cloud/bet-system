# 🚀 БЫСТРЫЙ ЗАПУСК - Анализ + Telegram

Виктор, здесь всё готово. Инструкция по использованию:

---

## 📝 Что было создано

| Файл | Назначение |
|------|-----------|
| `analyze_and_send_telegram.py` | ⭐ Основной скрипт (анализ + отправка) |
| `send_fixed_analysis.py` | Альтернатива с повторными попытками |
| `ahk_trigger_handler.py` | Обработчик сигналов от AutoHotkey |
| `TELEGRAM_INTEGRATION_GUIDE.md` | Полное руководство |

---

## 🎯 Три способа использования

### 1️⃣ Запустить анализ сейчас (самый простой)

```bash
python analyze_and_send_telegram.py
```

**Что происходит:**
- Анализирует 4 тестовых матча
- Отфильтровывает только подходящие (неничейные, фаворит ведет)
- Форматирует красивое сообщение
- Отправляет в @TrueLiveBet
- Сохраняет копию в `last_telegram_message.txt`

**Результат тестирования:**
```
✅ Матч 1: АЗ Алкмаар vs ПСВ Эйндховен - ПОДХОДИТ
✅ Матч 2: Боде Глимт vs Брюн - ПОДХОДИТ
⚪ Матч 3: Сент Трюйден vs Стандард Льеж - НИЧЬЯ, пропускаем
✅ Матч 4: Фрайбург vs Санкт-Паули - ПОДХОДИТ

📈 Результаты: 3 подходящих матчей из 4
```

---

### 2️⃣ Отправить с повторными попытками (более надежно)

```bash
python send_fixed_analysis.py
```

**Отличие:**
- Если первая попытка отправить неудачна → автоматический повтор
- Ждет 2, 4, 8 секунд между попытками
- 3 попытки всего

---

### 3️⃣ Интеграция с AutoHotkey (для автоматизации)

```bash
# Запустить анализ по сигналу от AHK
python ahk_trigger_handler.py run

# Или мониторить сигналы в фоне
python ahk_trigger_handler.py monitor
```

---

## 📊 Логика анализа

```
ВХОДНЫЕ ДАННЫЕ: список матчей (team1, team2, score, coef_p1, coef_p2, ...)
        ↓
1️⃣ ФИЛЬТР 1: Исключаем ничейные (0-0, 1-1 и т.д.)
        ↓
2️⃣ ФИЛЬТР 2: Определяем фаворита (по коэффициентам)
        ↓
3️⃣ ФИЛЬТР 3: Проверяем: ведет ли фаворит?
        ↓
4️⃣ ЕСЛИ ДА: добавляем в рекомендации
        ↓
ВЫХОД: список готовых рекомендаций
        ↓
ФОРМАТИРОВАНИЕ: красивое сообщение для Telegram
        ↓
ОТПРАВКА: POST запрос к Telegram API в @TrueLiveBet
```

---

## 🎨 Пример готового сообщения

```
🎯 LIVE-АНАЛИЗ • 19:43 МСК, 09.11.2025

—————————————

⚽ ФУТБОЛ ⚽

1. АЗ Алкмаар vs ПСВ Эйндховен

   Счет: 0-3 (1Т, 29 мин) | Нидерланды. Эредивизи

   🎯 Рекомендуем: ПСВ Эйндховен

   📊 Обоснование:
   • ПСВ Эйндховен - фаворит (🔥 кэф 1.03)
   • На поле контролирует (счет 0-3)

   💰 Кэф BetBoom: ~1.03

2. Боде Глимт vs Брюн

   Счет: 1-0 (1Т, 16 мин) | Норвегия. Элитсерия

   🎯 Рекомендуем: Боде Глимт

   📊 Обоснование:
   • Боде Глимт - фаворит (🔥 кэф 1.03)
   • На поле контролирует (счет 1-0)

   💰 Кэф BetBoom: ~1.03

3. Фрайбург vs Санкт-Паули

   Счет: 2-1 (2Т, 89 мин) | Германия. Бундеслига

   🎯 Рекомендуем: Фрайбург

   📊 Обоснование:
   • Фрайбург - фаворит (💪 кэф 1.50)
   • На поле контролирует (счет 2-1)

   💰 Кэф BetBoom: ~1.50

—————————————

📌 Важные моменты:
  • Все рекомендации основаны на анализе лайв-данных
  • Ставим только на матчи где фаворит лидирует
  • Размер ставки - только из собственного банка

—————————————

⚠️ Дисклеймер: Беттинг связан с рисками. Анализируйте самостоятельно перед ставкой.

🤝 @TrueLiveBet — честный ИИ-анализ лайв-ставок
```

---

## 🔧 Интеграция с Browser MCP

Когда будешь интегрировать реальные данные с BetBoom:

**Сейчас (тестовые данные):**
```python
matches_data = [
    {'team1': '...', 'team2': '...', 'score': '...', ...}
]
main(matches_data)
```

**После интеграции с MCP:**
```python
from browser_based_analyzer import get_real_betboom_data

matches_data = get_real_betboom_data()  # Парсит BetBoom через Browser MCP
main(matches_data)
```

---

## 📋 Структура проекта

```
Backtothestart/
│
├── analyze_and_send_telegram.py          ⭐ ОСНОВНОЙ СКРИПТ
│   ├── analyze_matches()                 - фильтрация + анализ
│   ├── format_telegram_message()         - красивое форматирование
│   └── send_to_telegram()                - отправка в канал
│
├── send_fixed_analysis.py                📱 АЛЬТЕРНАТИВА (с повторами)
│
├── ahk_trigger_handler.py                🤖 АВТОМАТИЗАЦИЯ
│   ├── run_analysis()                    - запустить анализ
│   └── monitor_for_signal()              - ждать сигналы от AHK
│
├── config.json                           ⚙️ КОНФИГ
│   └── telegram.bot_token, channel_id
│
└── TELEGRAM_INTEGRATION_GUIDE.md         📚 ПОЛНОЕ РУКОВОДСТВО
```

---

## 🐛 Возможные проблемы и решения

| Проблема | Решение |
|----------|---------|
| `Timeout` при отправке | Увеличить таймаут в `send_to_telegram()` |
| `Invalid token` | Проверить `config.json` и `BOT_TOKEN` |
| `Chat not found` | Убедиться что `CHANNEL_ID = '@TrueLiveBet'` |
| `Connection refused` | Проверить интернет |

---

## 📊 Логирование

**Файлы логов:**
- `last_telegram_message.txt` - последнее отправленное сообщение
- `ahk_trigger.log` - лог всех операций (при использовании `ahk_trigger_handler.py`)

---

## ✨ Для автоматизации (AutoHotkey)

**1. В `cursor_autosend.ahk` добавить:**
```autohotkey
; Каждые 45 минут отправить сигнал для анализа
SetTimer, SendAnalysisSignal, 2700000  ; 45 минут в миллисекундах

SendAnalysisSignal:
    ; Отправить запрос в Cursor чат
    PostMessage(WM_COPYDATA, "🎯F", ...)
return
```

**2. Cursor будет получать сигнал и запускать:**
```bash
python ahk_trigger_handler.py run
```

---

## 📝 Примеры использования

### Пример 1: Один раз запустить сейчас
```bash
python analyze_and_send_telegram.py
```

### Пример 2: Тест с конкретными матчами
```python
# test_my_matches.py
from analyze_and_send_telegram import main

my_matches = [
    {
        'team1': 'Команда А',
        'team2': 'Команда Б',
        'score': '2-0',
        'league': 'Любая лига',
        'time': '1Т, 20 мин',
        'coef_p1': 1.2,
        'coef_p2': 5.0
    }
]

main(matches_data=my_matches)
```

### Пример 3: Запланировать через Task Scheduler
```batch
# run_analysis_scheduled.bat
cd D:\cursor\Backtothestart
python analyze_and_send_telegram.py
```

---

## ✅ Checklist запуска

- ✅ Скрипт `analyze_and_send_telegram.py` создан
- ✅ Скрипт `send_fixed_analysis.py` обновлен
- ✅ Обработчик AHK `ahk_trigger_handler.py` создан
- ✅ Telegram BOT_TOKEN в конфиге (7824400107:...)
- ✅ CHANNEL_ID = '@TrueLiveBet'
- ✅ Логирование включено
- ✅ Формат сообщения соответствует шаблону

---

## 🚀 ГОТОВО К ИСПОЛЬЗОВАНИЮ!

Запусти сейчас:
```bash
python analyze_and_send_telegram.py
```

И проверь сообщение в @TrueLiveBet! 📱✨

