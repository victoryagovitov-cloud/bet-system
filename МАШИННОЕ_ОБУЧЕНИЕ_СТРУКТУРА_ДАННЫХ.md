# 🤖 МАШИННОЕ ОБУЧЕНИЕ - СТРУКТУРА ДАННЫХ

## 🎯 ЦЕЛЬ

Собирать данные в структурированном виде для:
- **Анализа паттернов** - что работает лучше всего
- **Обучения ML моделей** - предсказание вероятности успеха
- **Улучшения системы** - оптимизация префильтра
- **Автоматической оценки** - какой прогноз надежнее

---

## 📊 СТРУКТУРА ДАННЫХ ДЛЯ ML

### Каждый прогноз содержит:

```json
{
  "id": 1,
  "timestamp": "2025-10-29T14:30:00",
  "date": "2025-10-29",
  "time": "14:30",
  
  // Матч
  "sport": "football",
  "team1": "Спортинг",
  "team2": "Алверка",
  "league": "Португалия. Кубок лиги",
  
  // Счет на момент прогноза
  "score_at_prediction": "2:0",
  "score_team1": 2,
  "score_team2": 0,
  "minute": 55,
  "half": 2,
  
  // Ставка
  "odds": 1.01,
  "category": "dead",
  "recommendation": "П1",
  
  // ═══ ML ПРИЗНАКИ ═══
  
  "ml_match_stats": {
    "xg_team1": 1.24,
    "xg_team2": 0.15,
    "xg_diff": 1.09,
    "possession_team1": 62,
    "possession_team2": 38,
    "possession_diff": 24,
    "shots_team1": 4,
    "shots_team2": 2,
    "shots_on_target_team1": 3,
    "shots_on_target_team2": 1,
    "corners_team1": 2,
    "corners_team2": 1
  },
  
  "ml_h2h": {
    "team1_wins": 0,
    "draws": 0,
    "team2_wins": 1,
    "total_matches": 1,
    "team1_win_rate": 0
  },
  
  "ml_form": {
    "team1_wins_last5": 3,
    "team1_draws_last5": 1,
    "team1_losses_last5": 1,
    "team1_win_rate": 60,
    "team2_wins_last5": 2,
    "team2_draws_last5": 1,
    "team2_losses_last5": 2,
    "team2_win_rate": 40
  },
  
  "ml_context": {
    "is_late_match": false,
    "score_diff": 2,
    "is_dominant": true,
    "odds_category_numeric": 5
  },
  
  // Результат
  "status": "won",
  "final_score": "3:0",
  "final_score_team1": 3,
  "final_score_team2": 0,
  "checked_at": "2025-10-29T16:45:00",
  "profit": 1.0,
  
  "ml_result": {
    "predicted_correctly": true,
    "score_changed": true,
    "goals_conceded": 0,
    "goals_scored": 1
  }
}
```

---

## 🔢 ЧИСЛОВЫЕ ПРИЗНАКИ ДЛЯ ML

### 1. **Статистика матча:**
- `xg_team1`, `xg_team2` - Expected Goals
- `xg_diff` - Разница xG
- `possession_team1`, `possession_team2` - Владение (%)
- `possession_diff` - Разница владения
- `shots_team1`, `shots_team2` - Удары
- `shots_on_target_team1`, `shots_on_target_team2` - В створ
- `corners_team1`, `corners_team2` - Угловые

### 2. **H2H (история встреч):**
- `team1_wins` - Победы команды 1
- `draws` - Ничьи
- `team2_wins` - Победы команды 2
- `total_matches` - Всего матчей
- `team1_win_rate` - Процент побед команды 1

### 3. **Форма команд:**
- `team1_wins_last5` - Победы из последних 5
- `team1_draws_last5` - Ничьи
- `team1_losses_last5` - Поражения
- `team1_win_rate` - Процент побед

### 4. **Контекст прогноза:**
- `odds` - Коэффициент
- `odds_category_numeric` - Категория (1-5)
- `minute` - Минута матча
- `half` - Тайм (1 или 2)
- `is_late_match` - 80+ минута
- `score_diff` - Разница в счете
- `is_dominant` - Доминирование (xG diff > 1.0)

### 5. **Результат (target):**
- `won` - 1 (выиграл) или 0 (проиграл)
- `profit` - Прибыль в рублях

---

## 📈 ПРИМЕРЫ АНАЛИЗА

### После накопления данных (100+ прогнозов):

#### 1. **Анализ xG:**
```python
# Какой xG diff дает 90%+ вероятность?
high_xg_diff = [p for p in data if p['xg_diff'] > 1.5]
win_rate = len([p for p in high_xg_diff if p['won']]) / len(high_xg_diff)
# Результат: "xG diff > 1.5 → 95% побед"
```

#### 2. **Анализ минуты:**
```python
# На какой минуте прогнозы надежнее?
by_minute = {
    '45-60': [p for p in data if 45 <= p['minute'] <= 60],
    '60-75': [p for p in data if 60 < p['minute'] <= 75],
    '75+': [p for p in data if p['minute'] > 75]
}
# Результат: "60-75 минута → 88% побед"
```

#### 3. **Анализ формы:**
```python
# Как форма влияет на успех?
good_form = [p for p in data if p['form1_wins'] >= 3]
bad_form_opponent = [p for p in data if p['form2_wins'] <= 1]
# Результат: "3+ побед команды 1 + 1- побед команды 2 → 92%"
```

#### 4. **Оптимизация префильтра:**
```python
# Какие комбинации признаков дают лучший результат?
patterns = analyze_winning_patterns(data)
# Результат: обновить префильтр на основе реальных данных
```

---

## 🛠️ ИСПОЛЬЗОВАНИЕ

### 1. **Логирование с ML данными:**

```python
from prediction_logger_ml import PredictionLoggerML

logger = PredictionLoggerML()

# При отправке прогноза
pred_id = logger.add_prediction_ml(match_data, scores24_stats)

# Сохраняется полная ML структура!
```

### 2. **Обновление результата:**

```python
# После завершения матча
logger.update_prediction_result_ml(
    prediction_id=pred_id,
    final_score="3:0"
)

# Автоматически определяет won/lost
# Рассчитывает прибыль
# Добавляет ML метрики результата
```

### 3. **Экспорт для анализа:**

```python
# Экспорт в формат для pandas/ML
logger.export_for_ml('ml_dataset.json')

# Использование в Python
import pandas as pd
df = pd.read_json('ml_dataset.json')

# Анализ
print(df.groupby('xg_diff_category')['won'].mean())
```

---

## 📊 ФОРМАТЫ ЭКСПОРТА

### JSON (текущий):
```json
[
  {"won": 1, "xg_diff": 1.09, "odds": 1.01, ...},
  {"won": 0, "xg_diff": 0.3, "odds": 1.50, ...}
]
```

### CSV (опционально):
```csv
won,xg_diff,odds,possession_diff,minute,...
1,1.09,1.01,24,55,...
0,0.3,1.50,5,80,...
```

### Pandas DataFrame (для анализа):
```python
df = pd.DataFrame(logger.get_ml_dataset())
```

---

## 🎯 ПРИМЕНЕНИЕ ML

### Фаза 1: Накопление данных (сейчас)
- Сохранять каждый прогноз
- Проверять результаты
- Накопить 100+ записей

### Фаза 2: Анализ паттернов
- Какие признаки важнее?
- Какие комбинации надежнее?
- Оптимизация префильтра

### Фаза 3: ML модель (будущее)
- Обучить модель на данных
- Предсказывать вероятность успеха
- Автоматическая оценка надежности

### Фаза 4: Автоматизация
- ML модель оценивает каждый матч
- Автоматический выбор лучших
- Минимум ручной работы

---

## 📝 ВОПРОСЫ ДЛЯ АНАЛИЗА

После накопления данных ответим на:

1. **Какой xG diff оптимален?**
   - > 0.5? > 1.0? > 1.5?
   - Где баланс надежности и количества?

2. **Имеет ли значение минута?**
   - 45-60 мин vs 60-75 vs 75+
   - Когда прогнозы точнее?

3. **Владение = успех?**
   - Или важнее удары в створ?
   - Корреляция с победами

4. **H2H имеет значение?**
   - Или форма важнее?
   - Комбинация факторов

5. **Категории работают?**
   - МЕРТВЫЙ действительно 95%+?
   - Нужно ли корректировать пороги?

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Файлы:

**Логгер:**
- `prediction_logger_ml.py` - новый ML логгер
- `prediction_logger.py` - старый (совместимость)

**Данные:**
- `predictions_ml_log.json` - полные ML данные
- `predictions_log.json` - упрощенная версия
- `ml_dataset.json` - экспорт для анализа

**Анализ:**
- `ml_analysis.py` - скрипты анализа (создать позже)
- `ml_model.py` - ML модель (создать позже)

---

## ✅ ГОТОВО К ИСПОЛЬЗОВАНИЮ

**Создано:**
- ✅ `prediction_logger_ml.py` - ML логгер
- ✅ Структура данных определена
- ✅ Экспорт в ML формат
- ✅ Документация

**Добавлено в AutoHotkey:**
- ✅ Требование сохранять числовые данные
- ✅ Список признаков для ML
- ✅ Цели анализа

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. **Сейчас:** Использовать `prediction_logger_ml.py`
2. **Через неделю:** Накопить 50-100 прогнозов
3. **Через месяц:** Провести первый анализ
4. **Через 2 месяца:** Создать ML модель

---

**Данные = будущее системы!** 🎊

Каждый прогноз делает систему умнее! 🧠

